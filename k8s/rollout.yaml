apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: my-advanced-app-rollout
  namespace: app-blue
spec:
  replicas: 2
  selector:
    matchLabels:
      app: my-advanced-app
  template:
    metadata:
      labels:
        app: my-advanced-app
    spec:
      # AWS ECR authentication secret
      imagePullSecrets:
      - name: regcred
      containers:
      - name: my-app
        # Unified to us-east-1 region
        image: 692531238663.dkr.ecr.us-east-1.amazonaws.com/my-advanced-app:v9
        ports:
        - containerPort: 80
  strategy:
    blueGreen: 
      activeService: my-advanced-app-service
      previewService: my-advanced-app-preview-service
      autoPromotionEnabled: false